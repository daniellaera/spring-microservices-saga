spring:
  datasource:
    username: ${DB_USER:testuser}
    password: ${DB_PASSWORD}
  kafka:
    producer:
      properties:
        max.block.ms: 1000
        request.timeout.ms: 1000
        delivery.timeout.ms: 2000
